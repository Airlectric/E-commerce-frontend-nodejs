<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("../../partials/header"); %>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Orders</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    /* General Styles */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f8; /* Light grayish background */
      color: #333;
      line-height: 1.6;
    }

    .container {
      max-width: 1200px;
      margin: auto;
      padding: 20px;
    }

    /* Typography */
    h1, h2, h3, h4, h5, h6 {
      color: #003366; /* Navy Blue */
      font-weight: 600;
      margin-bottom: 15px;
    }

    /* Card Styles */
    .card {
      border: none;
      border-radius: 12px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08); /* Soft shadow */
      transition: box-shadow 0.3s ease;
    }

    .card:hover {
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1); /* Enhanced shadow on hover */
    }

    .card-header {
      background-color: #003366;
      color: #fff;
      padding: 15px 20px;
      border-radius: 12px 12px 0 0;
      font-weight: 500;
      font-size: 1.1rem;
      letter-spacing: 0.5px;
      display: flex; /* Add flexbox */
      justify-content: space-between; /* Distribute space between order text and order ID */
      align-items: center; /* Vertically align items */
    }

    .card-body {
      padding: 25px;
    }

    /* Order Card Specifics */
    .order-card .card-header h5 a {
      color: #ffc107; /* Light Gold */
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .order-card .card-header h5 a:hover {
      color: #fff; /* White on hover */
    }

    .badge {
      padding: 8px 12px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 500;
    }

    /* Badge Colors */
    .badge-pending {
        background-color: #ffc107; /* Light Gold/Yellow */
        color: #333; /* Dark text for visibility */
    }

    .badge-processing {
        background-color: #17a2b8; /* Info Color */
        color: #fff; /* Light text for visibility */
    }

    .badge-success {
        background-color: #28a745; /* Green */
        color: #fff;
    }

    .badge-failed {
        background-color: #dc3545; /* Red */
        color: #fff;
    }

    .badge-shipped {
        background-color: #007bff; /* Blue */
        color: #fff;
    }

    .badge-delivered {
        background-color: #28a745; /* Green - Same as Success */
        color: #fff;
    }



    /* Button Styles */
    .btn-primary {
      background-color: #003366;
      border-color: #003366;
      color: #fff;
      transition: background-color 0.3s ease, border-color 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #002244; /* Darker navy on hover */
      border-color: #002244;
    }

    .btn-warning {
      background-color: #ffc107; /* Light Gold */
      border-color: #ffc107;
      color: #212529;
      transition: background-color 0.3s ease, border-color 0.3s ease;
    }

    .btn-warning:hover {
      background-color: #d39e00; /* Darker gold on hover */
      border-color: #d39e00;
    }

    .btn-danger {
      background-color: #dc3545; /* Bootstrap danger color */
      border-color: #dc3545;
      color: #fff;
      transition: background-color 0.3s ease, border-color 0.3s ease;
    }

    .btn-danger:hover {
      background-color: #bb2d3b; /* Darker danger color on hover */
      border-color: #bb2d3b;
    }

    /* Form Styles */
    .form-select {
      border-radius: 8px;
      border: 1px solid #ced4da;
      padding: 10px;
    }

    .form-label {
      color: #495057;
      margin-bottom: 5px;
      font-weight: 500;
    }

    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 40px;
      border: 1px dashed #ccc;
      border-radius: 10px;
      margin-top: 30px;
    }

    .empty-state a {
      color: #003366;
      text-decoration: none;
    }

    .empty-state a:hover {
      text-decoration: underline;
    }

    /* Footer */
    footer {
      margin-top: 50px;
      text-align: center;
      color: #777;
      padding: 20px;
      border-top: 1px solid #ddd;
    }

    .icon-gold {
      color: #ffc107;
    }

    /* Utility classes */
    .text-navy {
      color: #003366 !important;
    }

    .bg-navy {
      background-color: #003366 !important;
    }

    .order-text { /* Style for "Order" text */
      font-weight: bold;
      font-size: 1.1rem;
      color: #fff; /* White color */
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center text-navy mb-4" style="font-family: 'Arial', sans-serif; font-weight: bold;">My Orders</h1>

    <!-- Filters and Sorting -->
    <div class="card mb-4 shadow-sm">
      <div class="card-body">
        <form method="GET" action="/order/orders" class="row g-3">
          <div class="col-md-6">
            <label for="timeFilter" class="form-label">Sort By:</label>
            <select class="form-select" id="timeFilter" name="timeFilter">
              <option value="Recent First" <%= timeFilter === 'Recent First' ? 'selected' : '' %>>Recent First</option>
              <option value="Oldest First" <%= timeFilter === 'Oldest First' ? 'selected' : '' %>>Oldest First</option>
            </select>
          </div>
          <div class="col-md-6">
            <label for="paymentStatusFilter" class="form-label">Payment Status:</label>
            <select class="form-select" id="paymentStatusFilter" name="paymentStatusFilter">
              <option value="All" <%= paymentStatusFilter === 'All' ? 'selected' : '' %>>All</option>
              <option value="Pending" <%= paymentStatusFilter === 'Pending' ? 'selected' : '' %>>Pending</option>
              <option value="Processing" <%= paymentStatusFilter === 'Processing' ? 'selected' : '' %>>Processing</option>
              <option value="Success" <%= paymentStatusFilter === 'Success' ? 'selected' : '' %>>Success</option>
              <option value="Failed" <%= paymentStatusFilter === 'Failed' ? 'selected' : '' %>>Failed</option>
            </select>
          </div>
          <div class="col-12">
            <button type="submit" class="btn btn-primary">
              <i class="fas fa-filter icon-gold me-2"></i> Apply Filters
            </button>
          </div>
        </form>
      </div>
    </div>

    <!-- Orders List -->
    <% if (orders.length > 0) { %>
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
      <% orders.forEach(order => { %>
      <div class="col">
        <div class="card order-card shadow-sm">
          <div class="card-header bg-navy">
            <h5 class="mb-0">
              Order ID:
              <a href="/order/order/<%= order._id %>" class="text-warning text-decoration-none">
                <%= order._id %>
              </a>
            </h5>
          </div>
          <div class="card-body">
            <p><strong class="text-navy">Total Amount:</strong> $<%= order.totalAmount.toFixed(2) %></p>
            <p>
              <strong class="text-navy">Payment Status:</strong>
              <span class="badge badge-<%= order.paymentStatus.toLowerCase() %>">
                <%= order.paymentStatus %>
              </span>
            </p>
            <p>
              <strong class="text-navy">Status:</strong>
              <span class="badge badge-<%= order.status.toLowerCase().replace(" ", "") %>"> <!-- Remove spaces if status has spaces -->
                <%= order.status %>
              </span>
            </p>
            <p><strong class="text-navy">Created At:</strong> <%= new Date(order.createdAt).toLocaleDateString() %></p>
            <p>
              <strong class="text-navy">Products:</strong>
              <%= order.productNames.length > 0 ? order.productNames.join(", ") : "No products available" %>
            </p>
          </div>
          <div class="d-flex justify-content-end p-3">
            <% if (order.paymentStatus === 'pending') { %>
            <a href="/order/checkout/<%= order._id %>" class="btn btn-sm btn-warning me-2">
              <i class="fas fa-dollar-sign"></i> Make Payment
            </a>
            <% } %>
            <form action="/order/order/<%= order._id %>/delete" method="POST">
              <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this order?')">
                <i class="fas fa-trash-alt"></i> Delete Order
              </button>
            </form>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
    <% } else { %>
    <div class="text-center mt-5 empty-state">
      <p class="text-muted">You have no orders yet. <a href="/order/" class="text-navy">Start shopping now!</a></p>
    </div>
    <% } %>
  </div>

  <%- include("../../partials/footer"); %>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" integrity="sha512-9usAa10IRO0HhonpyAIVpjrylPvoDwiPUiKdWk5t3PyolY1cOd4DSE0Ga+ri4AuTroPR5aQvXU9xC6qOPnzFeg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</body>
</html>
