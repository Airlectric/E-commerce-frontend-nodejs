<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-6">
        <img src="<%= product.image || '/default-image.png' %>" class="img-fluid rounded" alt="<%= product.title %>">
      </div>
      <div class="col-md-6">
        <h1 class="mb-3"><%= product.title %></h1>
        <p class="text-muted"><%= product.category %></p>
        <p><strong>Price:</strong> $<%= product.price.toFixed(2) %></p>
        <p><strong>Quantity Available:</strong> <%= product.quantity %></p>
        <p><%= product.description %></p>
        <form id="order-form" action="/orders" method="POST">
          <input type="hidden" name="productId" value="<%= product._id %>">
          <div class="mb-3">
            <label for="quantity" class="form-label">Quantity</label>
            <input type="number" name="quantity" id="quantity" class="form-control" min="1" max="<%= product.quantity %>" value="1" required>
          </div>
          <button type="submit" class="btn btn-primary">Order Now</button>
        </form>
        <a href="/products" class="btn btn-secondary mt-3">Back to Products</a>
      </div>
    </div>
  </div>
</body>
</html>
