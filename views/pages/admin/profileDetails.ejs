<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-4">
    <h1 class="text-center">Profile Details</h1>
    <div class="card shadow">
      <div class="card-body">
        <h5><b>Name:</b> <%= profile.name %></h5>
        <p><b>Age:</b> <%= profile.age || "N/A" %></p>
        <p><b>Location:</b> <%= profile.location || "N/A" %></p>
        <img src="<%= profile.profileImageUrl || 'placeholder.jpg' %>" alt="Profile Image" class="img-fluid rounded-circle">
      </div>
    </div>
  </div>
</body>
</html>
